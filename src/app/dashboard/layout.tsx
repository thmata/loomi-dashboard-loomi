
export const metadata = {
    title: "Dashboard - Início"
}

export default function DashboardLayout({ children }: any) {
    return (
        <html lang="en">
            <body>
                {children}
            </body>
        </html>
    )
}