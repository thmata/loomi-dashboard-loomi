import Início from '@/components/Dashboard/Início';
import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <Início />
        </div>
    );
};

export default Dashboard;